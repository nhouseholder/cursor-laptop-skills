---
name: clean-architecture-rebuild
description: Same behavior; new seams / folders / decoupling. Convert code to clean architecture.
---

Read `skills/clean-architecture-rebuild/SKILL.md` in this plugin (also at `~/.cursor/skills/clean-architecture-rebuild/SKILL.md`) and execute it now.

`$ARGUMENTS` is the feature / path to rebuild, and/or `--report-only`.
