---
name: performance-optimize
description: Performance engineer loop — find the bottleneck, then ship a measured fix against an explicit target.
---

Read `skills/performance-optimize/SKILL.md` in this plugin (also at `~/.cursor/skills/performance-optimize/SKILL.md`) and execute it now.

`$ARGUMENTS` is the scope and/or `--report-only`.
